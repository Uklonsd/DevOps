Функція get_disk_usage:
Приймає один аргумент - шлях до директорії.
Використовує команду du -ab, щоб отримати розмір файлів та папок у байтах.
Команда sort -nr сортує результати в порядку спадання.
Команда head -n 10 обмежує вивід першими десятьма рядками.

Змінні dir1 та dir2:
Задають директорії, для яких потрібно визначити об'єм зайнятого дискового простору.

Виклик функції get_disk_usage:
Викликається для кожної з директорій, виводячи інформацію про перші десять папок або файлів.
